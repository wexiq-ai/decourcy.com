# GmailCleaner page gate (Phase 1)
GMAIL_CLEANER_PASSWORD=changeme

# Neon serverless Postgres (Phase 1)
# https://neon.tech — create a project, copy the pooled connection string
DATABASE_URL=postgresql://user:password@host/db?sslmode=require

# Inngest background jobs (Phase 1)
# https://app.inngest.com — create an environment, copy keys
INNGEST_EVENT_KEY=
INNGEST_SIGNING_KEY=

# Google OAuth for Gmail (Phase 2)
# GOOGLE_CLIENT_ID=
# GOOGLE_CLIENT_SECRET=
# GOOGLE_REDIRECT_URI=https://decourcy.com/api/oauth/google/callback

# OAuth refresh-token encryption (Phase 2)
# TOKEN_ENCRYPTION_KEY=

# Anthropic API for classification (Phase 3)
# ANTHROPIC_API_KEY=
